name: Test

on:
    pull_request:
        types: [opened]

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: ./
              with:
                  token: ${{ secrets.GITHUB_TOKEN }}
                  user: '["kyoya0819"]'